name: 📊 JSL LOF 每日监控（纯文本邮件）

on:
  schedule:
    - cron: '30 6 * * *'   # UTC 6:30 = 北京时间 14:30
  workflow_dispatch:        # 允许手动触发测试

jobs:
  run-and-email:
    runs-on: ubuntu-latest
    steps:
      - name: 🛎️ Checkout code
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          pip install requests pandas openpyxl

      - name: ▶️ Run your script and capture output
        run: |
          python jsl_lof_analysis.py > output.log 2>&1
          echo "REPORT<<EOF" >> $GITHUB_ENV
          cat output.log >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: 📤 Send email via 163 (plain text body, no attachment)
        env:
          EMAIL_USER: ${{ secrets.EMAIL_USER }}       # 例如：yourname@163.com
          EMAIL_PASS: ${{ secrets.EMAIL_PASS }}       # 163邮箱的SMTP授权码
          RECIPIENT_EMAIL: ${{ secrets.RECIPIENT_EMAIL }}
        run: |
          python -c "
import os, smtplib
from email.mime.text import MIMEText
from email.mime.multipart import MIMEMultipart

subject = '📈 JSL LOF 每日分析报告（纯文本）'
body = '''${{ env.REPORT }}'''
to_email = os.getenv('RECIPIENT_EMAIL')
from_email = os.getenv('EMAIL_USER')
password = os.getenv('EMAIL_PASS')

if not all([from_email, password, to_email]):
    raise SystemExit('❌ 缺少邮件配置，请检查 Secrets')

msg = MIMEMultipart()
msg['From'] = from_email
msg['To'] = to_email
msg['Subject'] = subject
msg.attach(MIMEText(body, 'plain', 'utf-8'))

# 163 邮箱专用 SMTP 设置
server = smtplib.SMTP('smtp.163.com', 25)
server.starttls()
server.login(from_email, password)
server.sendmail(from_email, to_email, msg.as_string())
server.quit()

print('✅ 邮件已通过 163 邮箱发送成功！')
"